import { cafeCursorJohannesburgRecap } from '@/content/recaps/cafe-cursor-johannesburg';
import { RecapData } from '@/lib/types';

export const recapsBySlug: Record<string, RecapData> = {
	[cafeCursorJohannesburgRecap.slug]: cafeCursorJohannesburgRecap,
};
